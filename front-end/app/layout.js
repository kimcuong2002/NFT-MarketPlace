import "./globals.css";

import { NavBar, Footer } from "../components/componentsindex";

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <NavBar />
        {children}
        <Footer />
      </body>
    </html>
  );
}
